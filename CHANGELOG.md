# EasyAntigravity 更新日志

## v2.3.0 (2026-09-25)

### 🚀 灵动胶囊通知系统 (HUD Capsule)
- **极简极速原生渲染组件**：全新内置 `EasyAG-Resident` 独立高能驻留模块（体积仅 19.5 KB，基于原生 WPF 硬件加速与 Win32 无焦点穿透技术），0 控制台闪烁，内存占用 <25MB。
- **三大行动里程碑闭环**：
  - 🚨 **命中危险命令**：珊瑚红（`#fb7185`）霓虹脉冲光晕，精确显示拦截的危险指令文本与阻断提示，提供 `[前往审查 ↵]` 直达按钮。
  - 💡 **等待方案决策**：薄荷青（`#00f5d4`）霓虹光晕，精准捕获业务多选/单选题干，提供 `[前往选择 ↵]` 直达按钮。
  - ✅ **本轮任务完成**：翡翠绿（`#10b981`）呼吸光晕，生成就绪后常驻提醒，提供 `[前往查看 ↵]` 直达按钮。
- **常驻直至行动**：告别倒计时自动关闭导致的人工暂离漏看与任务无端卡死；右上角支持独立 `✕` 关闭按钮。
- **无感直达，绝不弹窗打扰**：点击胶囊按钮直接唤醒并前置聚焦 Antigravity 窗口，并平滑平移至高危/问答卡片；EasyAG 控制面板全程静默驻留，不弹窗抢夺用户正在进行的键鼠操作。

### 📥 系统托盘常驻与后台静默 (Tray Daemon)
- **启动自动静默**：用户在 EasyAG 面板完成代理/端口配置并点击「启动 Antigravity」后，主控制窗口自动隐藏退入系统托盘，全面转为静默守护模式。
- **面板收起控制**：顶部状态栏新增 `[ 📥 托盘静默 ]` 快捷按钮，支持随时手动收起。
- **系统托盘右键菜单**：
  - `🚀 打开控制面板`：双击或点击唤醒恢复主控制面板。
  - `🌐 浏览器控制台`：一键调出默认浏览器访问本地控制台。
  - `🛑 退出 EasyAntigravity`：彻底安全退出后台守护与本地代理。
- **Tauri 窗口生命周期升级**：点击主窗口右上角 `✕` 默认隐藏到托盘而非杀死代理进程，保障开发代理链路不间断。

### 🤖 任务完成状态机监控 (CDP State Machine)
- **输入框活动态指纹识别**：深度监听 Antigravity `[data-testid="agent-input-box"]` 内的 `<button data-tooltip-id="input-send-button-cancel-tooltip">` 取消按钮。
- **智能防抖空闲收敛**：Cancel 按钮消失且经过 1.2 秒稳定防抖无异常后，判定为本轮 Agent 操作完整结束，精准触发 `[EA_READY]` 任务完成胶囊。

### 🎨 界面细节与色彩规范增强
- **高危日志色彩归一**：统一高危熔断开关开启/关闭的控制台日志类别至 `tag-proxy`，消除操作时的色彩跳变。
- **状态词荧光增强**：控制台输出的「开启」与「停用」状态词采用专属霓虹荧光（`.glow-on` / `.glow-off`）微质感包裹。
- **端口输入框字体适配**：将自定义端口字体升级为跨平台一致的 Segoe UI / 系统中等字重，消除平台字体不匹配引起的视效违和。

## v2.2.1 (2026-09-23)

### 🎨 界面与体验优化
- **方案问答视觉标签**：新增薄荷荧光青（`#00f5d4`）高亮标识，明确区分权限审批请求与业务方案选择题。
- **方案问答日志去重**：根除多选卡片重复输出多条日志的现象，仅单次通知并暂停自动审批，静候人工决策。

### 🌐 汉化引擎双向还原
- **实时还原英文原版**：汉化开关关闭时，逆向字典与追踪机制自动将已汉化 DOM 节点还原回原生英文文本，无需刷新页面即可实现类似 Chrome 网页翻译的即时还原效果。

### 🧪 标准闭环测试工作流
- **8 阶段回归工作流**：新增完整的标准测试套件 `test/standard-workflow.test.js` 与 SOP 操作规程 `test/STANDARD_TEST_WORKFLOW.md`，覆盖自动审批、高危拦截、拦截后解锁、方案问答停止交互、问答后解锁、规则热重载、重载后拦截与全日志审计校验。
- **独立分发标准测试套件**：GitHub Actions 独立构建并分发测试套件产物，便于向用户验证系统的可靠性与闭环能力。

## v2.2.0 (2026-09-22)

### 🍏 macOS (Apple Silicon / Intel) 正式分发与内核修复
- **解决 Apple Silicon JIT 崩溃**：修复 M 系列（M1~M4）Mac 上 `easyag-node` 因 Hardened Runtime 缺失 JIT 权限在 V8 `ThreadIsolation::Initialize` 阶段直接触发 `SIGTRAP`（signal 5，退出码 133）的静默崩溃。通过 `Entitlements.plist` 注入 `com.apple.security.cs.allow-jit`、`allow-unsigned-executable-memory` 及 `disable-library-validation`。
- **规范应用外壳打包**：修复 CI 发布包解压后直接裸露 `Contents/` 的问题，现已封装为标准的 `EasyAntigravity.app` 外壳，解压即用。
- **CI 自动化 JIT 冒烟校验**：在 GitHub Actions macOS 构建流水线中加入 Node V8 执行自检，阻断任何无权限二进制产物的发布。

### 🛡️ 高危拦截与自动放行生命周期闭环
- **杜绝历史卡片告警死循环**：高危检测严格限定为当前页面上可见且处于待审批状态的活动按钮与其所属卡片，彻底根除历史已完成指令在聊天区域残留导致持续 800ms 告警锁死的问题。
- **手动放行即时释放锁定**：监听用户在页面上的显式点击（允许/拒绝/取消），用户放行后瞬间解除高危安全锁定，后续正常指令在 5~20ms 内无感恢复自动放行。
- **完整指令文本识别**：优化命令提取算法，过滤 `run`/`ran` 等多余前缀，完整显示待审批真实命令。

## v2.1.1 (2026-09-22)

### 🛡️ 高危规则与热重载
- **修复误报**：精准修正 `disk-wipe` 正则，彻底杜绝 PowerShell `Format-Table` / `Format-List` 被高危拦截。
- **动态热重载**：点击重载高危规则后即时广播到当前活跃的 CDP 会话，无需重启 Antigravity。
- **降低资源消耗**：移除后台 2 秒 periodic tick 轮询注入机制。

### ⚡ 审批卡状态机与防重入
- **WeakSet 内存生命周期**：摒弃生硬的 3 秒时间锁，以 DOM 根节点内存引用管理卡片生命周期，杜绝重复放行与日志丢失。
- **1:1 放行与请求对齐**：权限请求日志与自动放行日志严格精准对应。

### 🌐 交互区免翻保护与字典精简
- **全人机交互区免翻**：Agent 思考流（Thought）、对话流正文、文件审查步骤（Review）全面豁免翻译，彻底杜绝 React Virtual DOM 节点因外部文本修改导致的异常重绘与打字卡顿。
- **字典精简**：从 `ui_v2.json` 剔除高频泛化词条 `thinking` 与 `Review`，避免英文普通文本被拆词误伤。

### 🎯 自动化调试与界面优化
- **CDP 目标过滤**：严格过滤聊天嵌入子容器（`about:srcdoc`）与 Worker 线程，杜绝冗余 WebSocket 调试连接。
- **代理端口指引**：控制面板快捷代理端口升级为清晰文本指示（`7890 Mihomo`、`7897 Verge`、`10808 v2ray`）。

## v2.0.1 (2026-09-21)

### 🌐 原生免 TUN 链路
- **全面切换原生代理**：启动 Antigravity 时为 Chromium 传入本地 HTTP 代理，并向语言服务传递 `HTTP_PROXY` / `HTTPS_PROXY`。
- **不改系统代理**：保留本地回环直连，不需要 TUN，也不依赖 DLL 网络 Hook。
- **旧 DLL 兼容模式已弃用**：`version.dll` 注入与 AG 2.15.0 冲突；旧版客户端请使用历史 release，新版使用默认原生代理模式。

### 🛡️ 审批与高危风控
- **日志语义修正**：发现审批卡时记录“权限请求”；仅实际点击最终确认按钮后记录“放行”并增加批准计数。
- **授权范围默认收紧**：默认选择“仅允许本次”。
- **高危命令熔断**：高危规则命中时阻止自动操作并保留人工确认入口。

### 🎨 界面与分发
- **可读性优化**：采用高对比深色层级、明确边界与字体平滑处理，提升文字辨识度。
- **精简分发**：移除 WebView2 用户缓存、旧 DLL 兼容备份和未引用资源；保留系统 WebView2 Runtime 所需宿主文件、词典和规则。

### ⚠️ 兼容性
- Windows 11 通常自带 WebView2 Evergreen Runtime；少数缺失运行库的 Windows 10 设备需先安装 Microsoft Edge WebView2 Runtime。

## v1.2.0 (2026-09-20)

### 🎨 界面与窗口
- **WebView2 原生窗口**：不再依赖 Edge 浏览器，使用系统 WebView2 Runtime
- **自定义窗口**：标题栏显示 "EasyAntigravity"，带应用图标
- **exe 图标**：打包 EasyAG logo
- **单实例保护**：重复启动时聚焦已有窗口

### 🐛 修复
- **黑框问题**：所有 exec() 调用添加 windowsHide
- **退出逻辑**：点窗口 ✕ 关闭时后台进程同步退出
- **按钮状态**：启动中/运行中/已退出 三态实时切换
- **启动日志缓冲**：SSE 连接前的日志暂存补发

### 🌐 汉化引擎
- **词典更新**：3138 条词条
- **翻译禁区保护**：代码块、编辑器、终端不被误翻译
- **动态正则翻译**：审批选项中命令部分保持原文
- **调试模式**：--debug 参数开启 DOM 抓取

### ⚡ 自动审批
- **结构化检测**：data-testid + 结构指纹 + 关键字兜底
- **高危熔断**：危险命令自动拦截

### ⚠️ 已知问题
- **version.dll 与 AG 2.15.0 不兼容**
  - 开发者个人测试环境下，免 TUN 代理不支持 v2.15.0
  - 原因：AG 2.15.0 更新后 Electron 启动机制变化，现有 version.dll 需要重写适配
  - 临时方案：使用 AG 2.14.0 或开启 TUN 模式

### 📦 分发包
- WebView2Host 原生窗口宿主
- assets 精简为 2 个文件
- 含使用说明和版本兼容性提示
